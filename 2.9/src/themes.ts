import { createTheme } from "@mui/material";

const theme = createTheme({
    palette:{
        primary: {
            main: "#66bb6a"
        },
        secondary: {
            main: "#d50000"
        },
    }
}
)

export default theme;